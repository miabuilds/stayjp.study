# StayJP KOL 推薦碼 — 操作手冊

歸因型推薦碼:追蹤每個 KOL 帶來幾個付費,被推薦人**真實付費後**多送 7 天。web + App 通用。
獎勵 KOL 用「抽成」(線下結),系統算好給你數字。

---

## 1. 怎麼發碼?(先在後台建碼)

後台(admin email 登入)→ **訂閱者 tab → 🎟 推薦碼管理**:
1. 填「碼(如 HARRY)」+「KOL 名稱」+「抽成 %」(預設 20)→ 按**建立**(自動生 token)。
2. 建好的碼列在下面,每個有:
   - **複製推薦連結** → `https://stayjp.study/?ref=HARRY`(給網頁受眾,點了自動帶碼)
   - **複製 KOL 查詢頁連結** → 給 KOL 自己看成效(見 §3)
   - **停用**(碼失效後使用者就驗證不過)

兩種給法:
- **網頁受眾**:給推薦連結(`?ref=HARRY`)。
- **App 受眾**(重要):App 從 App Store 進來不帶連結 → 請對方「下載 App → 我的帳號 → 輸入碼 `HARRY`」。

> 碼**必須先在後台建立**才有效 —— 使用者輸入時會即時驗證,查無此碼會被擋下(見 §5)。

---

## 2. 你的後台怎麼看?

後台 → 訂閱者 tab:
- 最上方「🎟 推薦碼(KOL)」彙總:每碼帶來幾人 / 付費幾人。
- 「推薦碼管理」每個碼那行:**付費 N 人 · 預估抽成 NT$X**(已扣平台費、依平台分別算,見 §4)。
- 每位訂閱者那行 Email 後面也標 `🎟碼`。

---

## 3. 怎麼讓 KOL 看自己的成果?

每個碼有專屬「**KOL 查詢頁連結**」(後台複製):
`https://stayjp.study/kol.html?code=HARRY&t=<token>`

KOL 打開就看到即時的 **帶來人數 / 已付費 / 試用中**,不用你手動回報。
安全:靠 token 驗證,錯 token 看不到;只回彙總數字、不洩個資、不顯示抽成金額。

---

## 4. 抽成怎麼算?(利潤式,分平台)

**抽成 = 利潤 × 抽成%**,利潤 = 售價 − 平台手續費。手續費依平台不同 → 同售價下 App 抽得比網頁少(Apple 先咬走):
- **網頁(綠界)**:手續費約 **2.75%** → 利潤 ≈ 售價 × 0.9725
- **App(Apple,已加入 Small Business Program)**:抽成 **15%** → 利潤 ≈ 售價 × 0.85

**建議:抽成 20%、只抽首筆**(續訂全留給你)。各方案 20% 試算(後台自動算,已驗證):

| 方案 | 售價 | 網頁抽成 | App 抽成 |
|---|---|---|---|
| 月費 | 150 | 29 | 26 |
| 年費 | 1490 | 290 | 253 |
| 早鳥年費 | 990 | 193 | 168 |
| 買斷 | 2990 | 582 | 508 |

> 後台「預估抽成」= 目前 active 的被推薦人,各自 `售價×(1−平台費)×抽成%` 加總(首筆估算)。抽成%可每碼不同,建立時填、存在 `ref_codes.commission_pct`。

---

## 5. 被推薦人拿什麼 + 驗證

- **好處**:套用有效碼 + **真實付費訂閱成功** → 到期日自動 **+7 天**(我們的權益、不影響 Apple/綠界計費)。**只有真付款才送**(試用/沒付款不送),數位邊際成本≈0。一次性、不疊加。
- **驗證**:輸入碼按「套用」→ 即時查碼是否存在且啟用。
  - 有效 → 顯示「✓ 已套用 HARRY(KOL名)」並記下。
  - 查無 → 「查無此推薦碼,請確認後再輸入」,不會亂存。
  - 驗證服務暫時不可用 → 先記住(fail-open),不卡使用者。

---

## 6. 推薦碼輸入框在哪?(使用者端,手動打字、不一定要連結)

| 位置 | 誰看得到 |
|---|---|
| **方案頁 pricing**(四個方案卡正上方) | 所有人(含 App) |
| **我的帳號 account**(免費版區塊) | **只有免費用戶**(已付費/admin 看不到) |

套用後**不跳頁**,只在欄位下方顯示確認,留在原頁選方案。已套用會鎖定(首次歸因不可改)。

---

## 7. 賺錢提醒(很重要)

- **務必加入 Apple Small Business Program** → Apple 抽成 30%→**15%**(年營收 < 100 萬美金都符合)。
- **KOL 抽成只抽首筆**(20% 或固定),續訂全是你的;「賺很少」只在第一筆,看的是留存(LTV)。

---

## 速查
- 碼登錄:`ref_codes/{CODE}` = { kol, token, active, commission_pct, created_at }(admin 讀寫)
- 歸因:`users/{uid}.ref_code`(首次不覆蓋)
- 程式:`auth-header.js`(?ref 捕捉)、`index/account/pricing`(欄位+寫入)、`validateRefCode`(驗證)、`revenuecat-webhook`+`ecpay-callback`(付費後 +7)、`kolStats`(KOL 頁)、`admin.html`(管理+抽成試算)
- 防濫用:+7 只在真實付款發、一次性;碼需後台建立才驗證得過
- 每日稽核:`dailySubAuditCron` 揪「付費卻過期 / 該過期沒過期」,後台訂閱者 tab 頂部顯示
