# StayJP KOL 推薦碼 — 操作手冊

歸因型推薦碼:追蹤每個 KOL 帶來幾個付費,被推薦人付費後多送 7 天。web + App 通用。

---

## 1. 怎麼發碼?(沒有「建立」步驟,碼是自由的)

系統不需要先建碼 —— 你**自己決定**每個 KOL 一個碼(短、大寫、英數,如 `HARRY`、`TOFU`),然後給他們兩種入口:

- **網頁連結**(給網頁受眾):`https://stayjp.study/?ref=HARRY`
  點了瀏覽 → 推薦碼自動帶入、登入後自動歸因。
- **手動碼**(給 App 受眾,**重要**):請 KOL 說「下載 App → 我的帳號 → 輸入推薦碼 `HARRY`」。
  ⚠️ App 從 App Store 進來**不帶連結參數**,所以 App 用戶**只能靠手動輸入碼**歸因。

建議:碼大寫、每個 KOL 唯一、好記。大小寫不拘(系統自動轉大寫)。

---

## 2. 你的後台怎麼看?

後台(admin email 登入)→ **訂閱者** tab,最上方一條:

```
🎟 推薦碼(KOL):HARRY 帶來 12 人(付費 5) · TOFU 帶來 8 人(付費 2)
```

- **帶來 N 人** = 有這個碼、且已進入訂閱狀態(試用/付費/取消…)的人數。
- **付費 M** = 其中目前 active(真的在付費)的人數 → **照這個結 KOL 抽成**。
- 每位訂閱者那一行 Email 後面也會標 `🎟HARRY`。
- 注意:只「免費註冊、從沒訂閱」的人不會出現在訂閱者清單(這裡看的是有訂閱紀錄的人)。

---

## 3. 怎麼讓 KOL 看自己的成果?

**目前:手動**(MVP 階段沒有 KOL 自助後台)。你每月看後台 → 截圖/告知該 KOL「你帶來 X 付費,本期抽成 $Y」。少量 KOL 這樣最快。

**之後可做(stage 3,要再開發)**:KOL 自助頁 —— 給每個 KOL 一個帶 token 的網址(如 `/kol?code=HARRY&t=xxxx`),後端函式驗 token 後只回該碼的數字。需要時再說。

---

## 4. 被推薦人拿什麼 + 機制

- 套碼後**真實付費訂閱成功** → 到期日自動 **+7 天**(我們的權益,不影響 Apple/綠界計費)。
- **只有真的付款才送**(試用、沒付款不送)→ 不白送非付費者;數位服務多 7 天**邊際成本≈0**。
- **一次性**:`ref_bonus_at` 旗標,續訂不重發、不疊加。
- **兩條金流都發**:Apple(revenuecat-webhook)+ 綠界(ecpay-callback)。

---

## 5. 怎麼測試?

**A. 歸因(網頁)**
1. 無痕視窗開 `https://stayjp.study/?ref=TEST1`
2. 到方案頁,推薦碼欄位應**預填 TEST1**(在四個方案卡正上方)。
3. 用測試 Google 帳號登入 → 到「我的帳號」應顯示「已套用推薦碼:TEST1」。

**B. 歸因(App)**
1. App → 我的帳號 → 推薦碼欄位輸入 `TEST1` → 套用 → 顯示「✓ 已套用」。

**C. +7 天(要真的付款)**
1. App:用 sandbox Apple ID 訂閱;網頁:用綠界測試卡訂閱。
2. 付款成功後 → 我的帳號到期日 = 正常到期 **+7 天**,且顯示「(7 天好康已發放 ✓)」。

**D. 後台**
- 後台 → 訂閱者 → 上方出現「TEST1 帶來 1 人(付費 1)」。

> 純歸因(沒付款)只能在 Firestore `users/{uid}.ref_code` 或本人帳號頁看到;後台訂閱者清單只列有訂閱紀錄的人。

---

## 6. 推薦碼欄位在哪?

- **網頁 pricing(方案頁)**:四個方案卡**正上方**的「🎟 有推薦碼?」卡片。`?ref=` 連結進站會自動預填。
- **帳號頁(account,App+網頁通用)**:免費版區塊裡,「查看方案」上面。App 用戶在這裡輸入碼。
- 套用後**不跳頁**,只在欄位下方顯示綠字確認,留在原頁選方案。已套用會鎖定顯示(首次歸因不可改)。

---

## 7. 賺錢提醒(很重要)

- **先加入 Apple Small Business Program** → Apple 抽成 30%→**15%**(年營收 < 100 萬美金都符合)。沒加等於每筆多付一倍。
- **KOL 抽成建議只抽「第一筆」**(20% 或固定額),續訂全是你的。
- 「賺很少」只發生在第一筆(Apple + KOL 同時咬);第二期起就健康 → 看的是留存(LTV)。

---

## 速查
- 碼寫在 `users/{uid}.ref_code`(首次歸因不覆蓋)
- 程式:`auth-header.js`(?ref 捕捉)、`index.html`/`account.html`/`pricing.html`(寫入+欄位)、`revenuecat-webhook`+`ecpay-callback`(+7 天)、`admin-list-subscribers`+`admin.html`(統計)
- 防濫用:+7 只在「真實付款」發、一次性
