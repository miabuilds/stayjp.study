# AI 功能規劃 + 成本估算（給 Mia 過目）

> 前提:三個 AI 功能**全部鎖付費 + 每人每日上限 + 全站當日總額熔斷**。
> 因為「偶爾高流量」的多半是免費用戶,而 AI 只有付費用戶能用 → **流量爆量那天 AI 成本幾乎不動**,真正要防的是付費用戶把額度用滿。

## 官方定價（2026-08 查證,USD / 每百萬 token）
| 模型 | 輸入 | 輸出 | 適合 |
|---|---|---|---|
| Haiku 4.5 | $1 | $5 | 便宜、量大(每日建議、評估) |
| Sonnet 5（8/31 前優惠） | $2 | $10 | 品質重要(口說批改);9/1 起漲成 $3/$15 |

---

## 架構共通點
- **API key 一定放後端**(Cloud Functions v2, asia-east1),絕不放前端 → 否則被盜刷。
- 每個 AI 呼叫先過:`isPremium()` → `每日額度檢查(Firestore)` → `全站當日熔斷檢查` → 才呼叫 Claude。
- 每次呼叫把 token 用量寫回 Firestore(`ai_usage/{yyyy-mm-dd}`),累加當日估算金額。
- system prompt 用 **prompt caching**(0.1x 讀取價)再省一筆。

---

## 功能一:每日讀書範圍建議 ✅ 最便宜、最好做、最安全
- **做什麼**:根據用戶程度 + 進度,每天給一份「今天讀這些」清單(單字範圍/文法點/一篇文章/一篇讀解)。
- **模型**:Haiku(甚至可先用純規則式,幾乎 0 成本,之後再加 AI 潤飾)。
- **單次成本**:輸入 ~2k + 輸出 ~0.8k ≈ **$0.006 / 人 / 天**。
- **每日上限**:1 次/天(本來就是每日一份)。
- **月成本**:100 位每日活躍付費用戶 × $0.006 × 30 ≈ **$18 / 月**。

## 功能二:AI level 評估 ✅ 便宜、好做
- **做什麼**:一組自適應小測 → AI 評你大概 N 幾 + 弱點 + 建議路線。
- **模型**:Haiku。
- **單次成本**:輸入 ~5k + 輸出 ~1.2k ≈ **$0.011 / 次**。
- **每日上限**:1 次/週(評估不用天天做)。
- **月成本**:150 人每月各評 1 次 ≈ **$2 / 月**。

## 功能三:AI 口說練習 ⚠️ 最吸引人、但最貴 + 最複雜 → 建議最後做
- **做什麼**:用戶說日文 → 轉文字 → AI 批改發音/文法/自然度 + 語音回覆 + 多輪對話。
- **技術難點(要先解決)**:
  1. **語音辨識(STT)**:Claude 不做語音轉文字,要接 Whisper API(~$0.006/分)或 Google STT。
  2. **AI 動態語音回覆(TTS)**:VOICEVOX 現在跑在你的 Mac 本機,**沒辦法直接當雲端服務**。動態回覆不能用預錄。要嘛把 VOICEVOX 架在便宜 VPS,要嘛接 TTS API(另計費)。← 這是口說最大的卡點。
- **模型**:Sonnet(批改品質重要)。
- **單次成本(一段對話 ~8 輪 all-in)**:LLM ~$0.11 + STT ~$0.02 + TTS ≈ **$0.15 / 段**。
- **每日上限**:建議 5 段/天(這項是成本 90% 來源,上限最關鍵)。
- **月成本**:60 位付費用戶每天各 2 段 × $0.15 × 30 ≈ **$540 / 月**。

---

## 月成本三情境（AI 只算付費用戶,與總流量無關）
| 情境 | 每日建議 | level 評估 | 口說 | 合計/月 |
|---|---|---|---|---|
| 保守(50 付費活躍) | $9 | $0.6 | ~$180 | **≈ $190** |
| 中等(150 付費活躍) | $27 | $1.6 | ~$540 | **≈ $570** |
| 只上功能一+二(不做口說) | $27 | $1.6 | — | **≈ $30** |

> 重點:**口說佔成本 9 成以上**。先上功能一、二,月成本壓在 ~$30 以內;口說等 STT/TTS 架構備好、且確定值得再開。

## 防爆量三道鎖
1. **付費牆**:免費用戶完全碰不到 AI(高流量那天多半是免費用戶 → AI 成本不動)。
2. **每人每日上限**:建議 1/天、評估 1/週、口說 5 段/天。
3. **全站當日熔斷**:當日估算金額超過上限(例如 $30/天)→ 全站 AI 當天暫停,顯示「今日 AI 額度已滿,明天再來」。隔天歸零。

## 建議上線順序
1. **每日讀書範圍建議**(先純規則,幾乎免費;再加 Haiku 潤飾)
2. **level 評估**(Haiku,便宜)
3. **口說練習**(解決 STT + 雲端 TTS 後才開,Sonnet + 嚴格每日上限)

---
*數字為估算,實際依真實 token 用量與付費活躍人數浮動。上線後用 `ai_usage` 每日回寫做真實監控。*
